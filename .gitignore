node_modules/
dist/
.env
*.local
*.log
.DS_Store
# Agent scratch — briefs, ledgers, review packages. Never committed.
.superpowers/

# SQLite — a run from the repo root resolves DATABASE_URL against cwd
*.db
*.db-wal
*.db-shm
data/

# Browser-automation output — screenshots, DOM snapshots, console logs.
# Agents write here deliberately (see CLAUDE.md); none of it is ever committed.
# Screenshots are evidence for one review, not repository content: they go
# stale the next time the UI changes and nothing ever updates them.
.mcp-assets/
.playwright-mcp/

# Agent worktrees — transient, and each is its own git repo.
.claude/
